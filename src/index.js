const Input = require('./input');
const {
  addReducer,
  dispatch,
  dispatchSet,
  getPathValue,
  getState,
  reducer,
  reduxSetup
} = require('./redux-easy');

module.exports = {
  Input,
  addReducer,
  dispatch,
  dispatchSet,
  getPathValue,
  getState,
  reducer,
  reduxSetup
};
